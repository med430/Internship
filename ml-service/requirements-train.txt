# CF training + serving deps — kept OUT of the main image so a rebuild can never fail over a
# LightFM build error. The serving model wrappers lazy-import these (missing → cfScore=0).
# Install only when you decide to train CF:  pip install -r requirements-train.txt
# (lightfm needs C build tools: gcc/g++; on slim images add build-essential first.)
scipy>=1.11,<2.0
implicit>=0.7,<1.0
lightfm>=1.17,<2.0
pytest>=8.0,<9.0
