% ─── Diagramme de classes — Phase 2 : Documents IA & Communication ──────────
\begin{tikzpicture}[
  assoc/.style={-{Stealth[length=5pt]}, draw=gray!70, thin},
  comp/.style={-{Diamond[open,length=7pt]}, draw=gray!70, thin},
  lbl/.style={font=\scriptsize, fill=white, inner sep=1pt, midway},
  mongo/.style={draw=green!50!black, fill=green!7, rounded corners=3pt,
                font=\small\bfseries, align=center, inner sep=6pt},
  ref/.style={draw=gray!50, fill=gray!6, rounded corners=3pt, dashed,
              font=\small\itshape, align=center, minimum width=3cm,
              minimum height=0.7cm},
]

% ── Rangée haute : CV  |  CoverLetter  |  Interview ─────────────────────────
\umlclass{cv}{0}{0}{4cm}{CV}{%
\textbf{PK} id : UUID\\
\textbf{FK} userId : UUID\\
fileName : String\\
fileUrl : String\\
createdAt : DateTime}{%
+upload()\\
+download()\\
+delete()}

\umlclass{cl}{5.5}{0}{4cm}{CoverLetter}{%
\textbf{PK} id : UUID\\
\textbf{FK} userId : UUID\\
fileName : String\\
fileUrl : String\\
createdAt : DateTime}{%
+generate()\\
+download()\\
+delete()}

\umlclass{itw}{11}{0}{4.8cm}{Interview}{%
\textbf{PK} id : UUID\\
\textbf{FK} studentId : UUID\\
persona : InterviewPersona\\
score : Float?\\
feedback : String?\\
duration : Int?\\
facialScore : Float?\\
facialSummary : String?\\
facialMetrics : JSON?\\
createdAt : DateTime}{%
+start()\\
+answer()\\
+generateReport()}

% ── Rangée basse : InterviewSlot ─────────────────────────────────────────────
\umlclass{slot}{5.5}{-8.0}{4.2cm}{InterviewSlot}{%
\textbf{PK} id : UUID\\
\textbf{FK} applicationId : UUID\\
\textbf{FK} proposedBy : UUID\\
proposedAt : DateTime\\
status : SlotStatus\\
confirmedAt : DateTime?}{%
+propose()\\
+accept()\\
+decline()}

% ── Énumérations ──────────────────────────────────────────────────────────────
% InterviewPersona — positionné à droite d'Interview
\node[draw=orange!60, fill=orange!8, rounded corners=3pt,
      font=\scriptsize, align=left, inner sep=5pt] at (15.2, -2.8) {
  \textbf{\guillemotleft enum\guillemotright}\\
  \textbf{InterviewPersona}\\
  EMPATHIC\\TECHNICAL\\DIRECT
};

% SlotStatus — positionné à droite d'InterviewSlot
\node[draw=orange!60, fill=orange!8, rounded corners=3pt,
      font=\scriptsize, align=left, inner sep=5pt] at (11.0, -9.0) {
  \textbf{\guillemotleft enum\guillemotright}\\
  \textbf{SlotStatus}\\
  PROPOSED\\ACCEPTED\\DECLINED\\CONFIRMED
};

% ── Références vers Phase 1 (entités externes) ────────────────────────────────
\node[ref] (user_ref)    at (2.7,  2.5) {User\\(Phase 1)};
\node[ref] (student_ref) at (11.0, 2.5) {StudentProfile\\(Phase 1)};
\node[ref] (app_ref)     at (5.5, -15.5) {Application\\(Phase 1)};

\draw[assoc, dashed] (cv_title.north)    -- node[lbl, right]{\scriptsize FK userId}    (user_ref.south);
\draw[assoc, dashed] (cl_title.north)    -- node[lbl, right]{\scriptsize FK userId}    (user_ref.south);
\draw[assoc, dashed] (itw_title.north)   -- node[lbl, right]{\scriptsize FK studentId} (student_ref.south);
\draw[assoc, dashed] (slot_meth.south)   -- node[lbl, right]{\scriptsize FK applicationId} (app_ref.north);

% ── MongoDB — représenté comme composant séparé ───────────────────────────────
\node[mongo, minimum width=4.2cm] (conv) at (0, -8.0) {
  \textbf{Conversation (MongoDB)}\\
  \rule{3.8cm}{0.3pt}\\[3pt]
  \ttfamily\scriptsize
  participantIds : String[\,]\\
  lastMessage : String?\\
  lastMessageAt : Date?\\
  \textit{timestamps: true}
};

\node[mongo, minimum width=4.2cm] (msg) at (0, -14.0) {
  \textbf{Message (MongoDB)}\\
  \rule{3.8cm}{0.3pt}\\[3pt]
  \ttfamily\scriptsize
  conversationId : String\\
  senderId : String\\
  content : String\\
  readBy : String[\,]\\
  \textit{timestamps: true}
};

% ── Relations internes ────────────────────────────────────────────────────────
% Conversation 1 --< N Message
\draw[comp] (msg.north) -- node[lbl, right]{N}
            node[lbl, near end, right]{1} (conv.south);

\end{tikzpicture}
