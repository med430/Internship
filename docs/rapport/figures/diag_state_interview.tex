% ─── Diagramme d'états — Simulateur d'entretien virtuel ─────────────────────
\begin{tikzpicture}[
  state/.style={rectangle, rounded corners=6pt, draw=primary,
                fill=primary!8, font=\small\bfseries,
                minimum width=3.5cm, minimum height=0.9cm, text centered},
  init/.style={circle, fill=primary, minimum size=0.35cm, inner sep=0pt},
  final/.style={circle, draw=primary, fill=primary!20,
                minimum size=0.45cm, inner sep=0pt},
  trans/.style={-{Stealth[length=5pt]}, thick, draw=primary},
  loop/.style={-{Stealth[length=5pt]}, thick, draw=orange!70!black},
  lbl/.style={font=\scriptsize, fill=white, inner sep=2pt},
  note/.style={draw=gray!40, fill=gray!8, rounded corners=3pt,
               font=\scriptsize, align=left, inner sep=5pt},
]

% ── État initial ──────────────────────────────────────────────────────────────
\node[init] (start) at (0,0) {};

% ── États ─────────────────────────────────────────────────────────────────────
\node[state] (setup)      at (0,-1.8)  {CONFIGURATION};
\node[state] (question)   at (0,-3.8)  {QUESTION POS\'{E}E};
\node[state] (recording)  at (0,-5.8)  {ENREGISTREMENT};
\node[state] (processing) at (0,-7.8)  {TRAITEMENT IA};
\node[state] (responding) at (0,-9.8)  {R\'{E}PONSE AUDIO};
\node[state] (scoring)    at (0,-11.8) {G\'{E}N\'{E}RATION SCORE};

% ── État final ─────────────────────────────────────────────────────────────────
\node[final] (end) at (0,-13.4) {};

% ── Transitions principales ───────────────────────────────────────────────────
\draw[trans] (start)      -- (setup);
\draw[trans] (setup)      -- node[lbl,right]{Persona + niveau choisis} (question);
\draw[trans] (question)   -- node[lbl,right]{\'{E}tudiant d\'{e}marre l'enregistrement} (recording);
\draw[trans] (recording)  -- node[lbl,right]{Envoi audio (WebSocket)} (processing);
\draw[trans] (processing) -- node[lbl,right]{Groq Whisper (STT) + LLM} (responding);
\draw[trans] (responding) -- node[lbl,right]{ElevenLabs (TTS) jou\'{e}} (question);
\draw[trans] (question)   -- ++(4,0) |- node[lbl,right]{Session termin\'{e}e} (scoring);
\draw[trans] (scoring)    -- node[lbl,right]{Rapport sauvegard\'{e}} (end);

% ── Boucle question–réponse ───────────────────────────────────────────────────
\draw[loop, dashed] (responding.west) -- ++(-2,0) |-
  node[lbl, left]{N questions (boucle)} (question.west);

% ── Notes de service ──────────────────────────────────────────────────────────
\node[note, text width=3.5cm] at (6,-5.8) {
  \textbf{ENREGISTREMENT}\\
  Capture micro navigateur\\
  $\to$ chunks audio envoyes\\
  en WebSocket
};

\node[note, text width=3.5cm] at (6,-8.8) {
  \textbf{TRAITEMENT IA}\\
  Whisper large-v3 $\to$ texte\\
  LLM $\to$ feedback + question\\
  (latence < 3s)
};

\node[note, text width=3.5cm] at (6,-11.8) {
  \textbf{R\'{E}PONSE AUDIO}\\
  ElevenLabs TTS $\to$ MP3\\
  Voix selon persona\\
  (Empathique / Tech / Direct)
};

% ── Accolade : analyse faciale parallèle ─────────────────────────────────────
\draw[decorate,
      decoration={brace, mirror, amplitude=5pt},
      gray!55, thick]
  (8.2,-3.8) -- (8.2,-11.8)
  node[midway, right=8pt, font=\scriptsize, gray!65,
       align=left, text width=2.8cm] {
    \textbf{Analyse faciale}\\
    (parall\`{e}le)\\[3pt]
    MediaPipe Face\\
    Landmarker\\[3pt]
    52 blendshapes\\
    1 \'{e}ch. / 400 ms\\
    10 m\'{e}triques\\
    aucun stockage vid\'{e}o
  };

\end{tikzpicture}
